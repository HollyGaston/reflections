What happens when you initialize a repository? Why do you need to do it?

This enables you to start tracking changes in Git.


How is the staging area different from the working directory and the repository? What value do you think it offers?

The staging area allows you to consolidate multiple files that you want to commit at the same time. 


How can you use the staging area to make sure you have one commit per logical change?

Group files in the staging area by logical change and then commit them all at once.


What are some situations when branches would be helpful in keeping your history organized? How would branches help?

Branches are helpful when you are making a different version of a program
and don't want to affect the existing version.


How do the diagrams help you visualize the branch structure?

The diagrams are a visual representation (like a tree) of the branches in a commit history.


What is the result of merging two branches together? Why do we represent it in the diagram the way we do?

When two branches are merged, the changes from both are incorporated into the master and the commit history from each is accessible from the tip of the master.


What are the pros and cons of Git’s automatic merging vs. always doing merges manually?

Git's automatic merging makes merging faster by completing non-ambiguous merges without the user's input.